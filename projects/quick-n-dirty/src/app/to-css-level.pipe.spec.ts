import { ToCssLevelPipe } from './to-css-level.pipe';

describe('ToCssLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCssLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
